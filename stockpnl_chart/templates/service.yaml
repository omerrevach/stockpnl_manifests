apiVersion: v1
kind: Service
metadata:
  name: {{ include "stockpnl_chart.fullname" . }}
  labels:
    {{- include "stockpnl_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stockpnl_chart.selectorLabels" . | nindent 4 }}
